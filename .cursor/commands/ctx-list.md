Experimental integration for listing ctx workstreams in Cursor.

Run:

```bash
ctx list
```

If `ctx` is unavailable, fall back to:

```bash
python3 scripts/ctx_cmd.py list
```

Return the list directly.
