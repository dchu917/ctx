---
description: Experimental OpenCode command to open the ctx browser frontend
---

Experimental integration for opening the ctx browser frontend in OpenCode.

Run:

```bash
ctx web --open
```

If `ctx` is unavailable, fall back to:

```bash
python3 scripts/ctx_cmd.py web --open
```
